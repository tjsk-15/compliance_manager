.DS_Store
*.pyc
__pycache__/
*.egg-info/
.eggs/
dist/
build/
node_modules/
.idea/
.vscode/
*.swp
*.log
.env

# NOTE: the frontend build outputs (compliance_manager/public/frontend/ and
# compliance_manager/www/compliance.html) are intentionally COMMITTED so they
# deploy with the app on frappe.cloud (Press doesn't run `yarn build`).
# Rebuild with: cd frontend && yarn build
